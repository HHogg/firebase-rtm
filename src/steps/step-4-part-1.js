import firebase from 'firebase';


/**
 * @param  {string} rocketId Your ships identifier.
 * @param  {function} onSnapshot [description]
 *
 * @return {function} Function to remove the onSnapshot listener
 */
export default (rocketId, onSnapshot) => {

};
