import firebase from 'firebase';

/**
 * @param  {function} onSuccess Callback when token has been received
 * @param  {function} onError Callback when requesting permissions or token has failed
 *
 * @return {Promise} Promise resolved/rejected with token or error
 */
export default (onSuccess, onError) => {

};
