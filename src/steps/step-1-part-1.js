import firebase from 'firebase';

/**
 * @param  {function} onAuthStateChange Callback when auth state changes
 *
 * @return {function} Function to remove the onAuthStateChanged listener
 */
export default (onAuthStateChange) => {

};
