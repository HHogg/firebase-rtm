import firebase from 'firebase';

/**
 * @param  {function} onMessage Callback when a message is received
 *
 * @return {function} Function to remove the onMessage listener
 */
export default (onMessage) => {

};
