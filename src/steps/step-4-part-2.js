import firebase from 'firebase';

/**
 * @param  {string} rocketId Your ships identifier
 * @param  {string} launchCode Your launch code from step 3.
 */
export default (rocketId, launchCode) => {

};
